import org.springframework.stereotype.Service;

@Service
public class SpringChinoiserizService extends ChinoiserizService {
    public SpringChinoiserizService(ChinoiserizRepository chinoiserizRepository) {
        super(chinoiserizRepository);
    }
}
